0. 通用复用规则见 `skill.txt`（软链接到共享的 `../myutils/generalskill.txt`）；本文件只保留 `openclaw_docs` 项目特有要求。
1. 项目目的：同步并维护 `docs.openclaw.ai` 的本地 Markdown 镜像，在設置或查詢openclaw官方文檔時可以參考這些markdown文檔
2. 官网文档统一存放在 `docs/` 下；其中官网根级的分散 Markdown 文件，以及一些 section，统一归档到本地 `docs/others/`；其余文档尽量保持官网原有目录层级；抓取来源统一记录在根目录 `urls/`。
3. 发现本地 `.md` 为空、正文极少或明显截断时，优先视为抓取不完整，先重新下载，再使用内容。
4. 阅读openclaw文档时可以使用程序，先看标题层级，再看超链接，最后只对当前任务相关页面做深入阅读。
5. 如果本地 Markdown 链接值得讀時：本地已有就直接读；本地没有就先下載下來，再继续阅读或分析。
